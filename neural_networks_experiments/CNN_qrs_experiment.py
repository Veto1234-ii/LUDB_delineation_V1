# берем микродатасет (datasets.binary_datasets)
# обучаем сеть (neural_networks.CNN) + сохраняем график падения ошибки
# меряем F1 и среднее отклонение (neural_networks_helpers.helpers_CNN)
# сохраняем чекпоинт
# сохраняем картинки вида : нарисован сигнал, на нем правильная разметка данной точки и разметка от сети